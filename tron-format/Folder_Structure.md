# tron-format — folder layout

```
tron-format/
├── SKILL.md
├── Folder_Structure.md   ← this file
└── references/
    └── tron-conventions.md
```

Optional later: `assets/` for templates, `tools/` for retriever scripts — only if the skill grows.
