# SysMLEdgePrj open report

| Field | Value |
|-------|--------|
| Repo root | |
| `project.toml` name (projectId) | |
| `project.toml` repo | |
| Name matches directory | yes / no |
| `.gitmodules` | absent / updated (exit 0) / failed (exit code) |
| README SSOT kind | repo-based / SysMLEdge-based / missing |
| Overlay skill | none / `<id>` |
| `rev_status` | unbound / `working_ssot` + `rev.sha` + `rev.stale` + `memnetSession` / error text |
| Layout gaps | none / list |
| Next skill | `sysmledge-workflow` / also `sysmledge-cursor-multitask` / overlay id |

Do not add model structure, part trees, or proposal text to this report.
