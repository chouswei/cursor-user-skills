# Chat Shape (not SSOT)

After the second `pin_map`, answer with this block only (fill from stdout). Do not paste envelope JSON or leftover pipe tags.

## Plan

- **Goal:** (plan `:TSK` `goal`)
- **Status:** `in_progress` | `settled`
- **Session:** (session id, short)

## Steps (by `ord`)

| ord | wave | Goal | scope | Status | llm_id |
|-----|------|------|-------|--------|--------|

## Ready now

- (steps with empty `llm_id` whose predecessors are settled, or "none")
- **Run:** serial on this host | parallel wave `N` (Multitask on)

## Constraints

- (each active `:USR` `content`, one line)

## Open

- (each `:DEC` `code` still `open`, or "none")

## Changed this turn

- (one line: drafted | updated | repolished | settled | claimed wave)
