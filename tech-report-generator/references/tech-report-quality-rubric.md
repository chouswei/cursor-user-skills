# Tech report — self-review rubric (before final)

## Must pass

- **Audience fit:** sections match stated audience (team vs leadership vs handoff).
- **No invented metrics:** numbers only from user/supplied facts; else `[placeholder]` + gap called out.
- **Structure:** follows `assets/tech-report-template.md` section order; `#` title once.
- **Separation:** observation vs interpretation vs recommendation is visible (headings or labels).

## Should pass

- **Exec summary** present when body is long (> ~400 words equivalent).
- **Risks / open questions** explicit for handoff-style reports.

## Step-4 JSON

Emit: `pass` (bool), `violations` (string[]), `revision_note` (string). Max **1** revision.

## Retrieval seeds

tech report quality rubric audience metrics placeholder handoff structure
