# Analytical mechanics propose -- thin dictionary

Transfer these distinctions onto the caller's domain. Do not paste thesis equations.

## Resident versus inventory

- Inventory S: persistent cabinet (weights, corpus, session graph, ledger). Not the action.
- Resident W: what is actually in the active configuration this step.
- Action A: analysis integral over a trajectory of W. Usefulness is path-sensitive. A dump of S is not a better memory.
- Store: manifold of admissible configurations, not the selected point.
- Offered Shape X-tilde: what steering proposed. X-tilde sits inside W only if the caller admits the whole offer.

## Roles

| Role | Does | Does not |
|------|------|----------|
| Integrator | Realises the next step under admitted W | Pick the Lagrangian; own inventory |
| Steering | Chooses control u, H, force, or constraint | Generate; be the store |
| Plant | Holds inventory from which W is selected | Be the trajectory of W |

## Honesty and forgetting

- Native W is discrete. Continuous relaxation is an instrument; round back.
- Forgetting / eviction / lossy compression => open dissipative system. Closed symplectic flow cannot be the whole story.
- Discrete turn update and continuous dissipative account are different seams; do not run them as two competing eviction channels.

## Controls, caps, surfaces

- Control u is not a coordinate. Map any product cue q to u once at the boundary.
- Inequality caps stay hard. Shadow price / KKT estimator diagnoses biting versus slack; it is not a product knob.
- Three surfaces: proposal, admission, eviction. Rankers propose only.

## Gauge

- Identity is the element. Hidden names are unobservable.
- Rename invariance on unobservables. Admission order is physical.
- Label leak into rank / order / persistence is a gauge anomaly.

## Instantiations

Different products are different **controls** on one phase space, not new ontologies. STM ShapeWalk, RAG-style load, and KV eviction are the thesis worked examples -- copy the *split*, not the product verbs, onto the new plant.

## Firewalls (MUST NOT)

- Invent a destination-product SemVer (including MemNet **1.0**).
- Emit analysis fields (L, H, p, m, lambda, coverage) on a product read.
- Teach a dump of inventory as the memory API; confuse A with S.
- Soften hard caps to pretty-print a diagnostic.
- Collapse the three surfaces; sort by hidden id to "clean" W.
- Duplicate mcp-memnet install or memnet-format GQL in this skill's output.
- Treat product `Peak_L` as the Lagrangian (thesis Peak_L firewall).
- Add `rag_query` (or equivalent) to a MemNet wire because RAG is a legitimate load operator (thesis RAG firewall).

## Retrieval seeds

analytical mechanics propose, Lagrangian, Hamiltonian, KKT, shadow price, Noether, gauge, proposal admission eviction, resident versus inventory, dissipative forgetting, method transfer, phase space instantiation
