# memnet-format — folder layout

```
memnet-format/
├── SKILL.md
├── Folder_Structure.md   ← this file
└── references/
    └── memnet-wire-format.md
```

This is a pure `tool-wrapper` reference skill. No `assets/` or `tools/` are needed unless a retriever script is added later for very large tag vocabularies.