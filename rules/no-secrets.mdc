---
alwaysApply: false
---

# No secrets

- MUST NOT commit API keys, tokens, passwords, or credentials.
- MUST NOT echo or log secrets to stdout or commit-able files.
- MUST store secrets in environment variables or gitignored config (`.env`, `auth_config.json`, `secrets/`).
- MUST document expected env names and paths in README with placeholders only.
