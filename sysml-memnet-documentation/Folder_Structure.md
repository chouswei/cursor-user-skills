# sysml-memnet-documentation — folder layout

```
sysml-memnet-documentation/
├── SKILL.md
├── Folder_Structure.md   ← this file
└── references/
    └── sysml-memnet-patterns.md
```

Hybrid `pipeline` (primary) + `tool-wrapper` (secondary). The orchestration (goldfish loop, when-to-use, pairing) lives in `SKILL.md`; detailed atomisation examples and tag maps live in `references/`. No `assets/` or `tools/` required for current scope.