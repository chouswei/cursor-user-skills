# Output contract

**Step 5** emits **only** the final commit message as **plain text** (subject line; optional blank line and body). No markdown fences, no commentary, no `Token note` in the same block.
